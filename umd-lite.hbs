(function (u, m, d) {
	if (typeof define == 'function') {define('{{amdModuleId}}', [{{{amdDependencies.wrapped}}}], d);}
	else if (typeof exports == 'object') {module.exports = d({{{cjsDependencies.wrapped}}});} 
	else {u[m] = d({{{globalDependencies.normal}}});}
}(this, '{{objectToExport}}', function({{dependencies}}) {

{{{code}}}
return {{objectToExport}};
}));
